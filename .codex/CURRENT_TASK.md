# CURRENT TASK

DATE=2026-09-21
BRANCH=exp/s1m2-reusable-pieces
STATUS=S1M2_V2_REUSABLE_OBJECTIVE_LOCAL_IMPLEMENTATION

The local `reusable_pieces_v2` implementation uses role-collapsed
phonological-form identity and host-keyed posterior expected usage. At pass
finalization it stores raw `C(q)`, strongest-host `M(q)`, and learned
`R(q)=C(q)-M(q)`; scorer probability and complexity both use R. Piece roles
remain structural/diagnostic. Whole-form fallback, hard whitespace fences,
visible/internal joined-sandhi inversion, and transformed-event cost remain.
`reusable_pieces_v1` is historical failure state and cannot resume as V2.

Authority: `reports/core_methods/reusable_pieces/s1m2_v2_reusable_objective_20260921.md`.
No Full M0, representative/stress corpus, VM/cloud work, or long benchmark
was run. The next scientific gate is a researcher-selected bounded local DCS
experiment. V2 production contracts and qualification are deferred; do not
use V1 contracts as V2 authorization.
